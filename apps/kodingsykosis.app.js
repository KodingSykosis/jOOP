(function ($) {
    //This should load on document ready
    $.factory('shell', {
        singleton: true,
        
        //Entry point for all classes
        main: function() {
            
        }
    });
})(jQuery);